# 分布式文档转换系统 — 实施计划与测试路线

> **文档版本：v2.0（合并版）**
> **最后更新：2026-07-11**
> 目标：前端可视化裁剪 → 动态算力调度（Ubuntu 轻量 / Win11 重型，互为容灾）→ 健康检查接口对接个人看板。
> 原则：每个阶段独立可测、可回滚，不等前端画布完成才测后端。
>
> 本文件合并自 v1.0（Win11 初版）与 v1.1（Ubuntu 更新版），并同步了双端最新进度。

---

## 阶段划分总览

| 阶段 | 内容 | 前置依赖 | 状态 |
|---|---|---|---|
| 0 | 环境与基线确认 | 无 | ✅ 完成 |
| 1 | 健康检查接口（双端） | 阶段 0 | ✅ 完成 |
| 2 | Ubuntu 轻量处理服务 | 阶段 1 | ✅ 完成（docx/xlsx/PDF 全部测试通过） |
| 3 | Win11 重型 OCR 服务 | 阶段 1 | ✅ 完成（端到端验证通过，中文 100% 准确） |
| 4 | 调度层（路由 + 容灾切换） | 阶段 2、3 | ✅ 完成（8 项集成测试全部通过） |
| 5 | 前端裁剪 UI + 可选输出格式（md/txt/docx） | 可与 1-4 并行 | ✅ 完成（后端 + 前端均已实现） |
| 6 | 前后端联调 + 无落盘下载 | 阶段 4、5 | ⬜ 未开始 |
| 7 | 看板对接 | 阶段 1 | ⬜ 未开始 |
| 8 | 压测与容灾演练 | 全部完成 | ⬜ 未开始 |

---

## 技术栈与关键决策速查表

> 后续换新对话继续开发时，把这节内容贴给 Claude 就能补齐上下文。

**硬件与网络基线**

| 项目 | 值 |
|---|---|
| Ubuntu Server | 192.168.0.24，J3455，4GB 内存，Ubuntu Server 20.04，用户名 sergio7 |
| Win11 节点 | 192.168.0.81，i5-6400，16GB 内存，GTX 960（2GB 显存，Maxwell CC 5.2），Windows 11 |
| 内网延迟 | ping avg 0.381ms，0% 丢包 |
| 内网带宽 | iperf3 941 Mbits/sec（千兆满速） |
| 实际文件传输速度 | 约 94MB/s（机械硬盘瓶颈，调度层转发耗时估算用这个数） |

**已锁定的软件版本**

| 组件 | 版本/选型 | 备注 |
|---|---|---|
| 健康检查框架 | Flask + flask-cors + psutil | 双端通用 |
| 重型 OCR 引擎 | `paddlepaddle==2.6.2`（**CPU 版**）+ `paddleocr==2.7.0.3` | 2026-07-11 由 GPU 版切换为 CPU 版，零 CUDA 依赖 |
| OCR 模型系列 | PP-OCRv4 **mobile** 系列（非 server） | 峰值显存本可控制在 1.2GB 内；现已不使用 GPU，此约束仅作历史记录 |
| OCR 推理参数 | batch size = 1，`use_angle_cls=False` | 避免多模型同时加载吃满资源 |
| **OCR 实际运行模式** | **永久 CPU 模式（硬编码）** | GTX 960 Maxwell CC 5.2，PaddlePaddle 预编译 wheel 最低支持 sm_61(Pascal)，不含 sm_50/52，GPU 加速永久不可用 |
| numpy 版本约束 | `numpy<2.0.0` | PaddlePaddle 2.6.2 链接 numpy 1.x ABI |
| PDF 渲染 | PyMuPDF 1.28.0 | 仅用于页面渲染，不经过 paddleocr 内部 API，与其版本要求无冲突 |
| Win11 常驻方式 | **NSSM Windows 服务** | ⚠️ **架构决策变更**（见下方说明），2026-07-11 验证零 CUDA 依赖后 Session 0 完全安全 |
| Ubuntu 常驻方式 | systemd（现由 `app.py` 承担调度器角色） | |

> ⚠️ **架构决策变更记录**：项目早期（阶段 0-3 初版）曾因 rclone+WinFsp 的踩坑经验，明确要求 Win11 服务必须用「任务计划程序 / 登录时启动」而非 NSSM/Windows 服务，以避免 Session 0 隔离导致拿不到 GPU 上下文。
> 2026-07-11 发现：`paddlepaddle-gpu` 在 `import paddle` 时会加载 `cudart64_118.dll`，该 DLL 只在**用户 PATH**（不在系统 PATH），因此 Session 0（服务模式）下确实会 crash —— 这印证了最初的顾虑。
> 但由于 GTX 960 架构（Maxwell CC 5.2）本身就不被 PaddlePaddle wheel 支持 GPU 加速，团队决定**彻底切换到 `paddlepaddle-cpu`**（零 CUDA 依赖）。CPU 版本不加载任何 CUDA DLL，因此 Session 0 隔离问题不再存在，**NSSM 现在是安全的**，已改用 NSSM 部署（`install_nssm_service.bat`）。
> **结论**：只要 Win11 节点使用纯 CPU 推理（当前状态），NSSM 服务模式安全可用；如果未来升级到 Pascal+ 显卡并重新启用 `paddlepaddle-gpu`，需要重新评估 Session 0 风险，退回任务计划程序方案。

**重型任务技术路线**：PaddleOCR CPU 模式（原因见阶段 3）

**测试**
- [x] Ubuntu ↔ Win11 双向 ping 通（0% 丢包，0.381ms 均延迟）
- [x] 内网带宽千兆满速
- [x] 记录文件传输速度基线（94MB/s）
- [x] 确认 GTX 960 显存容量：2GB
- [x] 确认 CUDA 驱动版本：13.0（`nvidia-smi`），工具链 11.8（`nvcc`，现已可卸载）

---

## 接口与命名约定

| 项目 | 约定 |
|---|---|
| 健康检查路径 | `GET /health`，端口默认 5000（`PORT` 环境变量可覆盖） |
| 节点身份区分 | 环境变量 `NODE_NAME`（ubuntu/win11）+ `NODE_ROLE`（light/heavy） |
| `/health` 返回字段 | `status, node, role, cpu, mem, mem_available_mb, active_tasks, uptime_seconds, gpu_available, gpu_usable, ocr_mode` |
| `/health/gpu` | GPU 诊断信息；Win11 完整版，Ubuntu 简化版 `{gpu_available:false, gpu_usable:false, ocr_using_gpu:false}` |
| `/supported_formats` | `node, role, engine, formats, max_file_size_mb, max_pdf_pages, ocr_mode, capabilities`（含 `crop`、`output_formats`）— 供调度器动态发现节点能力 |
| `/convert` 引擎标识 | `paddleocr_cpu`（Win11）/ `markitdown`（Ubuntu），响应含 `engine`、`elapsed_ms`、`success`、`output_format`、`output_filename`、`mime_type`（docx 额外含 `file_base64`） |
| 错误响应格式 | 统一 `{"success": false, "error": "..."}` + HTTP 状态码 |
| 任务计数钩子 | `task_started()` / `task_finished()`，驱动 `/health` 的 `active_tasks` 字段 |
| CORS 策略 | 目前 `origins: "*"`，生产环境建议收紧为看板域名（`https://sergget.qzz.io`） |
| 探活超时阈值 | 建议 1-2 秒 |

---

## 部署路径约定

| 节点 | 代码路径 | 常驻方式 |
|---|---|---|
| Ubuntu | `/home/sergio7/markitdown/app.py`（调度器，已取代 `health_server_ubuntu.py`） | systemd（`markitdown.service`） |
| Win11 | `E:\Code\markitdown_selfhosted\ocr_server.py` | NSSM 服务（`Win11OCRService`，`install_nssm_service.bat` 一键部署） |

Ubuntu、Win11 双方代码库已拆分独立维护（Win11 仓库不再包含 `ubuntu/` 目录）。

---

## 阶段 0：环境与基线确认 ✅

**结论**
- 内网延迟/带宽不是瓶颈；机械硬盘 94MB/s 传输速度是调度层估算转发耗时的关键参数。
- GTX 960 Maxwell CC 5.2 不被 PaddlePaddle 2.6.2 预编译 wheel 支持，OCR 必须走 CPU 模式（已验证，识别精度不受影响）。
- 如未来升级到 Pascal+ 显卡（如 GTX 1060），服务架构支持切回 GPU 模式（改 `use_gpu=True` + 装 `paddlepaddle-gpu`），但需重新评估 NSSM/Session 0 风险。

---

## 阶段 1：健康检查接口（双端）✅

双端 `/health` 已实现并测试通过，CORS、探活超时判定均验证无误。

---

## 阶段 2：Ubuntu 轻量处理服务 ✅（2026-07-10 完成）

- docx/xlsx/PDF 转换全部测试通过
- 接口契约对齐 v1.1：`/health` 补全 `gpu_available`/`gpu_usable`/`ocr_mode`，新增 `/health/gpu`
- `/convert` 补全 `engine`/`elapsed_ms` 字段
- 并发 10 请求无故障，内存稳定

**待验证的历史测试项（未见明确记录，建议阶段 8 压测时补齐）**
- [ ] 边界测试：空文件、超大文件（J3455 4GB 内存下的失败阈值）

---

## 阶段 3：Win11 重型 OCR 服务 ✅（2026-07-11 端到端验证通过）

**关键结论**
- GTX 960 GPU 不可用的根本原因是**架构**（Maxwell CC 5.2 < wheel 要求的 sm_61），与 CUDA 驱动版本无关（`nvidia-smi` 显示的 13.0 是驱动支持上限，`nvcc` 显示的 11.8 是工具链版本，二者不冲突，只是架构层面 wheel 没有编译 Maxwell 的 kernel）。
- 代码已简化：移除 `_detect_gpu_info()` 等 ~100 行动态检测代码，硬编码 `GPU_AVAILABLE=True, GPU_USABLE=False, OCR_MODE="cpu"`。
- 彻底切换到 `paddlepaddle-cpu`（零 CUDA 依赖），CUDA Toolkit 可安全卸载，NSSM 部署因此变得安全（见上方架构决策变更说明）。

**端到端验证结果**

| 测试项 | 结果 | 说明 |
|---|---|---|
| `/health` 连通 | ✅ | `gpu_available: true`, `gpu_usable: false`（CC 5.2 不支持） |
| `/health/gpu` 诊断 | ✅ | CUDA 11.8 + Paddle 2.6.2，CC 5.2 硬限制 |
| OCR docx 拒绝 | ✅ | 返回 "Unsupported format"，仅支持图像/PDF |
| OCR 中文图片 | ✅ | 3 行中文 100% 准确，983ms |
| 接口契约 | ✅ | `/health`、`/convert` 均与契约一致 |

**双节点能力边界**

| | Ubuntu (light) | Win11 (heavy) |
|---|---|---|
| 引擎 | markitdown | paddleocr_cpu |
| 支持格式 | .docx .xlsx .pptx .pdf 等 | .png .jpg .bmp .tif .pdf .webp |
| 不支持格式 | — | .docx .xlsx .pptx（返回 400） |
| 首次性能 | 621ms（0.08MB docx） | 983ms（14KB 中文图片） |
| GPU | 无 | GTX 960（架构不受支持，未启用） |
| OCR 模式 | none | cpu |
| 内存可用 | 11.9 GB | 8.1 GB |

**未完成/待补充**
- [ ] 锁屏状态下服务是否正常（NSSM 用户账户运行，理论上不受影响，建议实测确认）
- [ ] 手动关闭服务，验证健康检查立刻反映 DOWN

---

## 阶段 4：调度层（路由 + 容灾切换）✅（2026-07-11 完成）

**路由策略（`app.py` 已实现）**

```
POST /convert (Ubuntu:5000) — 统一入口
  ├── .docx/.xlsx/.pptx/.txt/.csv … → 本地 markitdown
  ├── .png/.jpg/.bmp/.tif/.webp    → 转发 Win11 PaddleOCR
  └── .pdf                          → pdfplumber 分类
       ├── 文本型（可提取文字 ≥50 字符）→ 本地 markitdown
       └── 扫描型（无文字层）        → 转发 Win11 PaddleOCR
```

**新增端点**：`GET /supported_formats`（节点能力声明，供调度器动态发现）

**故障转移策略**
- 图像文件 + Win11 DOWN → 503 + 明确错误信息
- 扫描 PDF + Win11 DOWN → 本地 markitdown 降级（尽力提取文字）

**集成测试结果（8 项全部通过）**

| # | 场景 | 结果 | 详情 |
|---|---|---|---|
| 1 | `/supported_formats` | ✅ | 返回能力、格式列表、下游 OCR 节点 |
| 2 | docx → 本地 | ✅ | 716ms，engine=markitdown |
| 3 | 扫描 PDF → Win11 OCR | ✅ | 2918ms（3页），中文 ~98% 准确 |
| 4 | 文本 PDF → 本地 | ✅ | 正确识别文字层并路由到 markitdown |
| 5 | 图像 PNG → Win11 OCR | ✅ | 中文 100% 准确 |
| 6 | 不支持格式 .xyz | ✅ | 400 + 指向 `/supported_formats` |
| 7 | Win11 DOWN + 图像 | ✅ | 503 + failover 提示 |
| 8 | Win11 DOWN + 扫描 PDF | ✅ | 本地 markitdown 降级 |

**OCR 性能基线（Win11 PaddleOCR CPU）**

| 测试 | 页数 | 耗时 | 每页平均 |
|---|---|---|---|
| 中文扫描 PDF（167KB） | 3 | 2918ms | 973ms |
| 中文图片（14KB） | 1 | 983ms | 983ms |

**未完成/待补充（非阻塞，可留到阶段 8）**
- [ ] 抖动场景：连续 N 次失败才判定 DOWN（避免网络抖动误判切换）
- [ ] 切回验证：Win11 恢复后新任务优先切回，且不打断 Ubuntu 上正在处理的任务
- [ ] 雪崩测试：Win11 下线时连续提交多个重型任务，Ubuntu（4GB 内存）是否稳定不 OOM

---

## 阶段 5：前端裁剪 UI + 可选输出格式 ✅（2026-07-12 完成）

**做什么**
- 上传后不立即提交，先渲染预览画布。
- 图片：canvas 裁剪框。
- PDF：`pdf.js` 逐页渲染缩略图，支持单页调整或"应用到全部页"。
- 裁剪结果优先传坐标而非直接截图，保留 PDF 原文字层（能不 OCR 就不 OCR）。
- **输出格式选择**：`/convert` 新增可选 `output_format` 字段（`md` / `txt` / `docx`），默认 `md`。
  - `md`：原样返回 Markdown 内容。
  - `txt`：轻量剥离 Markdown 语法符号后返回纯文本。
  - `docx`：轻量解析 Markdown 结构（标题/列表/粗体/斜体/代码块），生成真实 .docx 文件，base64 编码返回。
  - 新增依赖：`python-docx`（已在 `requirements.txt` 中补充）。
  - ⚠️ **部署注意**：缺少 `python-docx` 会导致 `ModuleNotFoundError`，systemd 启动失败。

**测试**
- [x] 单页图片裁剪框拖拽、缩放交互正常
- [x] 多页 PDF：逐页裁剪 + 批量应用两种模式
- [ ] 裁剪坐标在不同缩放比例下换算回原始文件坐标系是否准确（建议做可视化调试模式比对）

---

## 阶段 6：前后端联调 + 无落盘下载 ⬜

**做什么**
- 前端定时/提交时探测 `/health`，走调度层统一入口（`app.py`）。
- 后端返回纯文本/JSON，前端用 Blob 触发下载，不写临时文件。

**测试**
- [x] 端到端跑通：上传 → 裁剪 → 提交 → 转换 → 浏览器下载，全程无残留文件
- [ ] 断网重连场景下前端探活逻辑不卡死，有明确错误提示

---

## 阶段 7：看板对接 ⬜

**做什么**：个人主页 JS 定时轮询两端 `/health`，渲染类 Uptime Kuma 的小绿点。

**测试**
- [ ] 两端在线/一端离线/两端离线三种状态显示是否符合预期
- [ ] 轮询频率不过高（建议 15-30 秒一次）

---

## 阶段 8：压测与容灾演练 ⬜

- [ ] 模拟"打游戏时关闭 OCR 服务"真实场景，全链路验证容灾切换 + 恢复
- [ ] `ab`/`locust` 对轻量接口做并发压测，确认 J3455 实际承载上限
- [ ] 记录"已知限制清单"（Ubuntu 兜底重型任务的最大并发数、降级/排队策略）
- [ ] 补齐阶段 2/3/4 中标记为"未完成"的边界测试项

---

## 待处理事项汇总

### 后续优化（非阻塞，可在阶段 5-8 期间穿插处理）
- [ ] 调度器缓存 Win11 `/supported_formats`（减少探活请求）
- [ ] 大文件传输耗时预估（`文件大小 / 94MB/s`），超阈值走本地降级
- [ ] 并发限流（J3455 内存有限，markitdown 并发建议 ≤3）
- [ ] 看板页面：实时展示双节点健康 + 路由统计
- [ ] 可选：新增 `GET /benchmark` 端点，返回节点性能基线数据供调度器权重计算

---

## 建议的开发顺序（更新）

1. ~~阶段 0-4~~ 已全部完成，双节点 + 调度层闭环已跑通。
2. ~~阶段 5~~ 前端裁剪 UI 已全部完成。
3. ~~阶段 6~~ 前后端联调已完成。
4. 阶段 7 看板对接工作量小，可与阶段 5/6 穿插进行。
5. 阶段 8 放在最后，同时把阶段 2/3/4 遗留的边界测试一并补齐。
